#!/bin/sh
set -e

CONFFILE=/etc/dibbler/client.conf

if [ "$1" = "purge" ] ; then
    if [ -e ${CONFFILE} ]; then
	rm ${CONFFILE}
    fi
    test -f /usr/bin/ucf && ucf --purge ${CONFFILE}
fi

#DEBHELPER#
